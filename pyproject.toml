[project]
name = "ublock-politics"
version = "0.1.0"
description = "Curated uBlock Origin filter lists that mute political and current-events noise."
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.uv]
package = false
